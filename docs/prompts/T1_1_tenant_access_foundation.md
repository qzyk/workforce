# Prompt — T1.1 Tenant Access Foundation

Use the previously approved T1.1 prompt.

Core objective:

Create `services/security/tenant_access.py`, add tests, document behavior, and integrate only one pilot route.

Rules:

- No schema changes.
- No broad route refactor.
- Preserve mode off behavior.
- Fail closed in strict mode.
- Add tests.
