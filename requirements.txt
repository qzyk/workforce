Flask>=3.0.0
Flask-SQLAlchemy>=3.1.1
Flask-Login>=0.6.3
Flask-WTF>=1.2.1
Werkzeug>=3.0.1
SQLAlchemy>=2.0.23
openpyxl>=3.1.2
reportlab>=4.0.7
pandas>=2.1.3
pypdf>=6.0.0  # parser PDF pentru eDevize Formular F3 (Faza 11.5)
xlrd>=2.0.1  # citire .xls binar (Excel 97-2003) pentru import devize
python-docx>=1.0.0  # generator DOCX pentru Procese Verbale (Faza 14)

# === IMPORT .MPP (MS Project binar) ===
# Formatul .mpp e binar proprietar; il citim via biblioteca Java MPXJ prin jpype.
# Necesita un JDK/JVM (Java 8+). Pe PythonAnywhere exista Java de sistem; daca
# nu, instaleaza un JDK complet din venv:  python -c "import jdk; jdk.install('17')"
# (NU jdk4py - JDK-ul lui minimal nu include modulul charsets cerut de MPXJ.)
mpxj>=16.0.0  # jar-uri Java pentru citit .mpp/.mpx/.xer + scris MSPDI XML
jpype1>=1.5.0  # bridge Python <-> JVM
install-jdk>=1.1.0  # helper optional pentru a instala un JDK complet din pip
Pillow>=10.4.0
python-dateutil>=2.8.2
APScheduler>=3.10.4
WTForms>=3.1.1
email-validator>=2.1.0

# === DEV / TESTING ===
pytest>=8.0.0
pytest-cov>=4.1.0
pytest-playwright>=0.4.4  # E2E browser tests (rulare optionala)

# === BIM / 3D / IFC ===
ifcopenshell>=0.7.0  # parser IFC pentru import modele BIM

# === DATABASE DRIVERS ===
# SQLite e built-in. Pentru MySQL pe productie (PythonAnywhere standard):
PyMySQL>=1.1.0  # driver MySQL pur Python, no compile needed

# === DB MIGRATIONS ===
# Alembic (Faza 1 BIM foundation): migratii versionate, backward-compatible.
# Pentru DB-uri existente: ruleaza 'alembic stamp head' o singura data.
alembic>=1.13.0
